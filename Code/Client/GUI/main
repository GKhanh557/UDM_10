#include <QApplication>
#include "ClientWindow.h"

int main(int argc, char *argv[])
{
    QApplication app(argc, argv);
    app.setApplicationName(QStringLiteral("UDM_10 Client"));

    ClientWindow window;
    window.show();

    return app.exec();
}
